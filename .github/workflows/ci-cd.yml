
name: CI/CD Multi-Branches

on:
  push:
    branches:
      - main
      - dev
      - 'feature/**'
  pull_request:
    branches:
      - main
      - dev

env:
  IMAGE_NAME: demo-devops

jobs:
  # JOB 1 : Build et Test (pour toutes les branches)
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout du code
      uses: actions/checkout@v3
    
    - name: 🔍 Afficher la branche
      run: |
        echo "🌿 Branche: ${{ github.ref_name }}"
        echo "📝 Commit: ${{ github.sha }}"
    
    - name: 🐳 Build de l'image Docker
      run: |
        docker build -t ${{ env.IMAGE_NAME }}:${{ github.ref_name }} .
        echo "✅ Image construite: ${{ env.IMAGE_NAME }}:${{ github.ref_name }}"
    
    - name: 🧪 Test du conteneur
      run: |
        echo "🚀 Démarrage du conteneur..."
        docker run -d -p 8080:80 --name test-container ${{ env.IMAGE_NAME }}:${{ github.ref_name }}
        sleep 5
        
        echo "🔎 Vérification du site..."
        curl -f http://localhost:8080 | grep "Pipeline DevOps"
        
        echo "🧹 Nettoyage..."
        docker stop test-container
        docker rm test-container
        
        echo "✅ Tests réussis!"
    
    - name: ✅ Résumé Build & Test
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ BUILD & TEST RÉUSSIS"
        echo "🌿 Branche: ${{ github.ref_name }}"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  # JOB 2 : Deploy to Docker Hub (UNIQUEMENT pour main)
  deploy-production:
    name: Deploy to Production
    needs: build-and-test
    runs-on: ubuntu-latest
    # ⚠️ Ce job s'exécute SEULEMENT sur la branche main
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: 📥 Checkout du code
      uses: actions/checkout@v3
    
    - name: 🔑 Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    
    - name: 🐳 Build de l'image de production
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest .
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }} .
    
    - name: 📤 Push vers Docker Hub
      run: |
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        echo "✅ Image publiée sur Docker Hub!"
    
    - name: 🎉 Résumé Déploiement
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🎉 DÉPLOIEMENT EN PRODUCTION RÉUSSI"
        echo "🐳 Image: ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest"
        echo "📦 Tag: ${{ github.sha }}"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  # JOB 3 : Notification pour dev
  dev-notification:
    name: Dev Environment Ready
    needs: build-and-test
    runs-on: ubuntu-latest
    # Ce job s'exécute SEULEMENT sur la branche dev
    if: github.ref == 'refs/heads/dev' && github.event_name == 'push'
    
    steps:
    - name: 📢 Notification Dev
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ ENVIRONNEMENT DEV MIS À JOUR"
        echo "🌿 Branche: dev"
        echo "📝 Prêt pour merge vers main"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"